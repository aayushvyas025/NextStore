import React, { Suspense } from "react";
import Layout from "../layout/Layout";

function Home() {
  return (
    <Suspense>
      <Layout minHeight={"100vh"}>
        
      </Layout>
    </Suspense>
  );
}

export default Home;
